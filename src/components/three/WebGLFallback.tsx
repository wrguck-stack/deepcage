import { BlueprintCar } from '../BlueprintCar'

/** The pre-existing technical illustration is deliberately retained for WebGL failures only. */
export function WebGLFallback() {
  return <BlueprintCar />
}
